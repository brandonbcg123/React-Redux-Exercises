import React from 'react';

const VideoListItem = (props) => {
    return <li>Video</li>;
    //each video list item which is intended to show a single video, for right now will just show a single <li> that says video.
};

export default VideoListItem;